/** Placeholder — tests d'intégration (webhooks mock) en Vague 3. */
import { describe, it, expect } from "vitest";

describe("integration placeholder", () => {
  it("suite prête", () => {
    expect(true).toBe(true);
  });
});
